// Starter data for a fresh install. Empty by default; populate to ship seed
// items (id -> item metadata) and their photos (id -> data-URL) with the app.
export const SEED_ITEMS = [];
export const SEED_IMAGES = {};
